<header>
     <div class="navbar navbar-default navbar-fixed-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to "StupidPoni", root_path, :class => 'navbar-brand' %>
          <p class="navbar-text">another one blog</p>
        </div>
        <div class="collapse navbar-collapse">
          <ul class="nav navbar-nav pull-right">
            <li<% if request.fullpath == "/" %> class="active" <% end %>><%= link_to "Home",    root_path %></li>
            <li<% if request.fullpath == "/about" %> class="active" <% end %>><%= link_to "About",    about_path %></li>
            <li<% if request.fullpath == "/contacts" %> class="active" <% end %>><%= link_to "Contacts",    contacts_path %></li>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </div>
</header>